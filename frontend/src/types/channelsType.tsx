export default interface exploreChannelType {
    id: string;
    type:string;
    password: string;
};

export default interface createChannelType {
    name:string;
    type: string;
    password: string;
}